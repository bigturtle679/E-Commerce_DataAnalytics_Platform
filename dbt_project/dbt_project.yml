name: 'ecommerce_platform'
version: '1.0.0'
config-version: 2

profile: 'ecommerce_platform'

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  ecommerce_platform:
    staging:
      +materialized: view
      +schema: staging
    analytics:
      +materialized: table
      +schema: analytics
